# 先看这个 —— PLAN 文件夹导航

这个文件夹里是我（AI）给你生成的**计划、排查文档和工具**。
**你不需要从文件名猜内容**，看下面这张表就够了。每天只需要打开 1 号文件。

---

## 一、想做什么 → 打开哪个

| 你想做什么 | 打开 / 运行 | 说明 |
|---|---|---|
| **今天/这周该干什么** | `1-当前计划-Python版8周.md` | ⭐ 每天只看这一个。8 周任务、每周检查点、AI 边界全在里面 |
| **看懂现在这份代码** | `5-代码精讲-逐行读懂mygit.py.md` | ⭐ 逐行讲原理 + Git 对象模型 + 自测 8 问，数字都实测过 |
| GitHub 登不上 / 该把代码传到哪 | `2-远程仓库与GitHub登录.md` | 已测出是链路问题不是密码问题，四条方案任选 |
| 想知道我的网络到底通不通 | 运行 `3-网络体检脚本.ps1` | 命令见下面第三节 |
| 以后想用 C++ 重写核心 | `4-第二阶段C++-暂不打开\` | **现在别打开**，等 Python 版做完再说 |
| 查某个报错是什么原因 | `1-当前计划-Python版8周.md` 的**附录 B** | "症状 → 原因"对照表，直接查 |

---

## 二、根目录里还有什么（每个东西是干嘛的）

整个项目根目录 `F:\MyGit` 只有这几样，没有堆积：

| 名称 | 是什么 | 要不要管 |
|---|---|---|
| `PLAN\` | 本文件夹：计划与工具 | 每天开 1 号文件 |
| `mygit.py` | **你的项目主程序**（原来的 `main.py`，按计划第 1 周 D2 改名） | 你写代码的地方 |
| `objects.py` / `repository.py` | 空文件，第 6 周重构拆模块时才用 | 现在不用管 |
| `test.txt` | 你之前测试用的临时文件，可以删 | 随意 |
| `test-sandbox\` | 手工试用区。**`mygit init` 只能在这里跑** | 已被 gitignore |
| `test-repo\` | 早期练习留下的假 `.git`，已被 gitignore | 可以直接删 |
| `.gitignore` | git 的忽略配置，**必须留在根目录**（挪进 PLAN 就失效了） | 别删、别挪 |
| `.git\` | 你自己的版本控制，已修好（`git fsck` 干净） | 别手动改 |

> `docs\设计笔记.md` 现在还不存在——那是计划里让你自己写笔记用的文件夹，第 3 周 D1 会让你建。

---

## 三、一分钟上手

```powershell
# 1) 看看网络和远程仓库该走哪条路
powershell -ExecutionPolicy Bypass -File F:\MyGit\PLAN\3-网络体检脚本.ps1

# 2) 打开今天的计划（把路径粘进 VS Code 也行）
code F:\MyGit\PLAN\1-当前计划-Python版8周.md

# 3) 保存进度
git -C F:\MyGit add -A
git -C F:\MyGit commit -m "整理: 文件收进 PLAN 文件夹"
```

---

## 四、用这个文件夹的规矩（免得以后又乱）

1. **只有 `0-先看这个.md` 是导航**，其余文件各自独立、按用途分号（1/2/3/4）。
2. 每个文件开头第一行都写明了它是什么、当前该不该看。
3. 我以后再生成新文件：**计划类**进 `1-` 或新的编号；**工具脚本**进 `3-` 或根目录；**第二阶段的东西**一律进 `4-`。不会再散在根目录。
4. 编号越大越"以后再看"：`0` 现在看，`4` 是几个月后的事。

---

## 五、当前一句话状态

你已选 **Python 8 周路线**。Python 版实际进度在 **第 2 周 Day 3**（`init`、blob 格式、SHA-1、zlib、`hash-object -w` 都已跑通）。

**已完成**
- ✅ GitHub 从零打通：建仓库 → 设身份 → 首次提交 → 浏览器授权 → 推送成功。凭据已存进 Windows 凭据管理器，以后 `git push` 不用再授权、不用令牌
- ✅ 第 1 周 Day 2：`main.py` 改名 `mygit.py`；`.git` 全部改成 `.mygit`；写 `HEAD` 加 `newline="\n"`（LF）
- ✅ 两项验收通过：`HEAD` 字节结尾是 `0A`（不再是 `0D 0A`）；`mygit hash-object` 与 `git hash-object` 结果完全一致

**下一步**
第 1 周 Day 3~4：在 `test-sandbox\` 里跑 `init`，用真实 git 观察 `.mygit\` 目录结构；然后画一张对象关系图（blob / tree / commit / ref / HEAD）。详见 `1-当前计划-Python版8周.md` 的第 1 周。
